---
# Ansible playbook
#

- name: Common settings
  hosts: all
  roles:
    - role: bootstrap
      tags:
        - bootstrap

    - role: mycooltools
      tags:
        - mycooltools

  tags:
    - common

...
